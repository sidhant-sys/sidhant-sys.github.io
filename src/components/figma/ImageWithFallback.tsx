import React, { useState } from 'react'

// Beautiful placeholder image with travel/destination theme
const PLACEHOLDER_IMG_SRC = 'https://images.unsplash.com/photo-1488646953014-85cb44e25828?ixlib=rb-4.0.3&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D&auto=format&fit=crop&w=400&q=80'

// Error fallback SVG
const ERROR_IMG_SRC =
  'data:image/svg+xml;base64,'

export function ImageWithFallback(props: React.ImgHTMLAttributes<HTMLImageElement>) {
  const [didError, setDidError] = useState(false)

  const handleError = () => {
    setDidError(true)
  }

  const { src, alt, style, className, ...rest } = props

  // Check if src is empty, undefined, or just whitespace
  const isEmpty = !src || src.trim() === ''
  
  // If src is empty, use placeholder; if error occurred, use error image
  const imageSource = isEmpty ? PLACEHOLDER_IMG_SRC : (didError ? ERROR_IMG_SRC : src)
  const imageAlt = isEmpty ? 'Travel destination placeholder' : (didError ? 'Error loading image' : alt)

  // If we're showing the error state, wrap in a container
  if (didError && !isEmpty) {
    return (
      <div
        className={`inline-block bg-gray-100 text-center align-middle ${className ?? ''}`}
        style={style}
      >
        <div className="flex items-center justify-center w-full h-full">
          <img src={ERROR_IMG_SRC} alt="Error loading image" {...rest} data-original-url={src} />
        </div>
      </div>
    )
  }

  // Normal image or placeholder
  return (
    <img 
      src={imageSource} 
      alt={imageAlt} 
      className={className} 
      style={style} 
      {...rest} 
      onError={isEmpty ? undefined : handleError} 
    />
  )
}
